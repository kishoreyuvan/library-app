
import { number } from 'library-app/helpers/number';
import { module, test } from 'qunit';

module('Unit | Helper | number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = number([42]);
  assert.ok(result);
});

